import { Outlet} from "react-router-dom";

const Layout = () => {
  return (
    <>
      <nav>
        <ul>
          <li>

          </li>
       
        </ul>
      </nav>

      <Outlet />
    </>
  )
};

export default Layout;